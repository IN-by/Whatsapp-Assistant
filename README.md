# WhatsApp Assistant

WhatsApp CRM 助手插件。

## 安装指南

**本插件即将发布到 Chrome 应用商店。**

（待补充 Chrome Web Store 链接）

## 开发与发布

### 版本管理
本项目使用 GitHub 进行代码版本管理。

### 发布流程
1. 修改 `manifest.json` 中的 `version`。
2. 运行 `./scripts/release.sh` 生成上传用的 ZIP 包。
3. 将 `dist/extension-vX.X.zip` 上传至 Chrome Web Store Dashboard。
